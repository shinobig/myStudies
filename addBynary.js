function addBynary(a, b){

  // crear i y j para viajar
  // crear variable de mas
  // ir de atras hacia adelante

  let i = a.length - 1
  let j = b.length - 1
  let shouldAdd  = false
  let constructor = ''

  while(i >= 0 && j >= 0){

    if(a[i] && b[i] === '1'){
      let char = '0'
      shouldAdd = true;
    }

  }


}

console.log(addBynary('11', '1'));

